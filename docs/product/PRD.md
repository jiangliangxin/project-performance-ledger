# 个人项目绩效收益记账系统 PRD

版本：V1.2  
状态：已完成业务梳理，可进入技术设计和原型阶段  
产品定位：个人项目绩效、公司收款节点和个人到账记录工具

## 1. 产品定位

本系统不是传统项目管理系统，而是一个个人起步、可供小团队低并发使用的绩效收益台账。它记录一次次与合作公司的业务合作，并追踪：

```text
合作公司 + 项目类型
        ↓
标准绩效和默认流程
        ↓
工作完成 / 公示 / 公司收款
        ↓
个人分批发放
        ↓
实际到账和待到账
```

第一阶段支持管理员创建少量成员账号。所有账号（包括管理员）只能查看和修改自己的合作记录；管理员额外负责账号和共享项目类型/规则管理，但不能查看或修改其他人的公司、记录、到账、导入数据、Dashboard 或导出。系统暂不实现团队任务协作、审批和细粒度组织权限。

## 2. 核心目标

- 快速记录合作公司和项目类型，不要求填写项目名称；
- 根据项目类型自动带出标准绩效和默认结算流程；
- 区分进行中、已完成、已公示、公司已收款和个人已到账；
- 支持预付款触发部分发放，但不擅自推算发放比例；
- 支持同一公司和类型多年后再次合作；
- 支持 Excel、CSV 和剪贴板批量录入；
- 保留规则版本、人工修改和到账历史；
- 让用户快速知道每条合作记录当前卡在哪个节点。

## 3. 业务边界

### 3.1 必须支持

- 合作公司管理；
- 项目类型和绩效规则管理；
- 合作记录管理；
- 项目完成和公示里程碑；
- 公司预付款、全款节点；
- 个人多批次到账；
- 绩效规则版本；
- 绩效金额人工覆盖；
- Excel/剪贴板规则导入；
- Dashboard 和基础统计；
- 数据导出和备份。
- 公网登录、管理员创建成员账号和按用户隔离数据。

### 3.2 第一阶段不做

- AI 识别或自然语言解析；
- 公司实际收款金额管理；
- 团队任务协作；
- 审批流；
- 工时管理；
- 项目成果、合同和附件管理；
- 短信、邮件和推送提醒；
- 复杂的自由编排工作流。

### 3.3 账号与数据隔离

- 公网部署必须登录；
- 不开放完全公开注册，由管理员创建、禁用和重置成员账号；
- 第一版只保留 `admin` 和 `member` 两种角色；
- 所有角色只能看到本人名下的合作公司、合作记录、到账和导入记录；
- 管理员可以管理账号及共享项目类型/规则，但没有跨用户业务数据读取或修改权限；
- 权限由后端执行，不能只依靠前端隐藏菜单；
- 项目类型和默认绩效规则由管理员统一维护，成员共同使用；
- 暂不实现成员之间的项目协作、共同编辑和共享记录。

## 4. 核心业务规则

### 4.1 合作记录

一条合作记录由以下信息组成：

```text
合作公司 + 项目类型 + 一次合作发生
```

不要求用户填写独立的项目名称。

同一公司和同一项目类型几年后再次合作，默认创建新的合作记录。系统可以提示可能重复，但不能自动合并。

展示名称由系统生成：

```text
甲公司｜DCMM评估｜2026-09
```

如果同一月份存在多条记录：

```text
甲公司｜DCMM评估｜2026-09（第1次）
甲公司｜DCMM评估｜2026-09（第2次）
```

### 4.2 日期

- 记录日期：用户首次创建或导入的日期；未填写时使用 `Asia/Shanghai（UTC+8）` 当前日期；
- 完成日期：项目工作完成日期，可以后续填写和修改；
- 公示日期：项目完成正式公示的日期；
- 预付款日期：公司收到预付款的日期；
- 全款日期：公司收到全款的日期；
- 个人到账日期：本人实际收到绩效的日期。

记录日期不自动等于完成日期。新建记录默认工作状态为“进行中”。

### 4.3 金额

页面只展示必要的金额：

| 字段 | 规则 |
|---|---|
| 标准绩效 | 项目类型在本人100%参与时的完整绩效金额 |
| 我的比例 | 独享默认100%，多人时可以是具体比例或待确认 |
| 我的应得 | 标准绩效 × 我的比例，或人工覆盖后的金额 |
| 已到账 | 所有个人到账记录之和 |
| 待到账 | 我的应得 - 已到账 |

多人项目比例未知时允许这样记录：

```text
标准绩效：10000元
我的比例：待确认
我的应得：待确认
已到账：3000元
```

实际到账可以先记录，之后补充比例。
此时当前节点仍为“待确认个人比例”，已到账金额单独显示，应得和待到账保持未知；不能因为已经有到账就显示为“部分发放”或计算为0。

### 4.4 工作、公示、收款和发放必须分开

合作记录包含四组状态：

#### 工作状态

- 进行中；
- 已完成。

归档是记录整理功能，不是工作状态或结算状态；归档只从默认列表隐藏记录，不删除、不改变金额、流程节点和到账历史，并支持恢复。V1 不提供“已取消”业务状态；历史库中的旧取消值保持只读，不批量改写。

#### 公示状态

- 不适用；
- 待公示；
- 已公示。

#### 公司收款状态

- 未知；
- 未收款；
- 已收预付款；
- 已收全款。

公司收款只记录节点和日期，不记录公司实际收款金额。

#### 个人发放状态

- 未发放；
- 待第一批发放；
- 部分发放；
- 待尾款发放；
- 已结清。

公司收款不等于个人到账。系统只有在新增个人到账记录后，才更新已到账金额。

## 5. 项目类型管理

项目类型是系统的规则中心，不只是分类字段。

### 5.1 类型规则字段

| 字段 | 说明 |
|---|---|
| 类型名称 | 如 DCMM评估、数据治理咨询 |
| 标准绩效 | 100%参与时的完整绩效金额 |
| 默认本人比例 | 独享默认为100% |
| 是否需要公示 | 是、否、待确定 |
| 公司收款节点 | 不要求、预付款、全款、预付款+全款 |
| 默认发放模式 | 完成/公示后一次发放、预付款后发放、预付款后部分发放+全款后发放剩余 |
| 生效日期 | 规则开始生效的日期 |
| 失效日期 | 可选 |
| 规则版本 | 如2026版 |
| 状态 | 启用、停用 |

### 5.2 规则示例

```text
项目类型：数据治理咨询
规则版本：2026版
标准绩效：10000元
默认比例：100%
需要公示：是
收款节点：预付款+全款
发放模式：预付款后部分发放、全款后发放剩余
```

### 5.3 规则应用

新建合作记录选择项目类型后，系统自动带出当前有效规则。

合作记录保存所使用的规则版本。以后修改项目类型规则时，历史合作记录不自动变化。

特殊合作记录可以人工覆盖：

- 标准绩效；
- 我的比例；
- 是否需要公示；
- 公司收款节点；
- 默认发放模式。

覆盖操作必须记录原因，不影响项目类型默认规则。

## 6. 流程和当前节点

### 6.1 典型流程

#### 交付后即可发放

```text
进行中 → 已完成 → 收款条件已满足 → 待个人发放 → 已到账
```

#### 公示后才可收款

```text
进行中 → 已完成 → 待公示 → 已公示 → 收款条件已满足
```

#### 预付款触发部分发放、全款触发剩余发放

```text
已公示或已完成
        ↓
公司已收预付款
        ↓
第一批个人发放可进行
        ↓
公司已收全款
        ↓
剩余个人绩效可发放
```

### 6.2 当前节点计算

系统根据规则和记录自动显示当前节点：

- 进行中；
- 待完成；
- 待公示；
- 待公司收款；
- 可发放第一批；
- 待个人发放；
- 部分发放；
- 待尾款发放；
- 已结清；
- 待确认个人比例。

里程碑允许乱序补录。例如历史数据可以先录入“已收全款”，之后再补完成日期和公示日期。系统不阻止保存，只提示缺失信息。

## 7. 发放批次和到账记录

### 7.1 发放批次

预付款和全款可以分别触发个人发放批次：

```text
第一批：预付款后部分发放
第二批：全款后发放剩余部分
```

每个批次可以记录：

- 批次名称；
- 触发条件；
- 发放比例；
- 计划金额；
- 实际到账金额；
- 到账日期；
- 备注。

发放比例和计划金额允许未知。

### 7.2 实际到账

每次个人收到绩效，都新增一条到账记录：

```text
到账批次
到账金额
到账日期
备注
```

系统累计所有到账记录，计算已到账和待到账。

## 8. 快速新增

### 8.1 最小输入

只要求：

```text
合作公司
项目类型
```

系统自动填入：

- 记录日期：北京时间当前日期；
- 工作状态：进行中；
- 标准绩效：读取项目类型规则；
- 我的比例：默认100%；
- 公示要求：读取项目类型规则；
- 收款要求：读取项目类型规则。

### 8.2 后续补充

可以之后补充：

- 完成日期；
- 公示日期；
- 我的比例；
- 特殊绩效金额；
- 预付款日期；
- 全款日期；
- 个人到账记录。

## 9. Excel 和剪贴板导入

不使用 AI，采用确定性规则解析。

### 9.1 支持格式

- 剪贴板粘贴或 Excel `.xlsx`、CSV/TXT 文件；
- 无表头时前两列为合作公司、项目类型，后续可选记录日期、特殊标准绩效和个人比例；
- 带支持表头别名的表格；
- 自动识别制表符、逗号或分号，不提供任意自定义分隔符或字段映射。

示例：

```text
甲公司    DCMM评估
乙公司    数据治理咨询
丙公司    系统建设
```

### 9.2 导入流程

```text
粘贴或上传
    ↓
选择数据格式
    ↓
按支持的表头别名或固定列顺序解析并匹配项目类型规则
    ↓
预览错误和重复项，选择要提交的有效行
    ↓
单事务提交所选行
```

### 9.3 导入规则

- 项目名称中包含空格时不能按空格自动拆分；
- 不依赖 AI；V1 采用确定性表头别名/列顺序，不提供任意字段映射；不符合格式的行在预览中报错；
- 类型匹配金额只作为规则带出，不需要额外确认；
- 按记录日期自动匹配当时有效的项目类型规则；没有可用规则的行报错且不可提交；
- 同公司和同类型重复时只提示，不自动合并；
- 仅勾选的有效行会提交，错误行不可选；重复项警告但不自动合并；一次提交在单个数据库事务中整体成功或整体回滚；
- 系统记录导入批次及新建记录 ID；V1 暂不提供批次浏览/整批撤销，录入错误可逐条归档且不删除事实；
- 未填写日期时使用导入当天北京时间日期。

## 10. 页面结构

### 10.1 Dashboard

展示：

- 进行中项目数；
- 已完成待公示；
- 已公示待收款；
- 已收款待个人发放；
- 部分发放；
- 已到账金额；
- 待到账金额；
- 已结清记录数。

不发送提醒，只展示当前状态和待处理节点。

### 10.2 合作记录列表

字段：

| 合作公司 | 类型 | 记录日期 | 工作状态 | 公示状态 | 公司收款 | 我的应得 | 已到账 | 当前节点 |
|---|---|---|---|---|---|---:|---:|---|

支持按以下条件筛选：

- 合作公司；
- 项目类型；
- 记录年度；
- 工作状态；
- 公示状态；
- 公司收款状态；
- 个人发放状态；
- 是否存在待确认比例。

### 10.3 合作记录详情

页面结构：

```text
合作公司和类型
金额概览
当前节点
工作/公示/收款里程碑
发放批次
实际到账记录
绩效调整历史
规则版本
```

### 10.4 项目类型管理

支持：

- 新增类型；
- 编辑类型；
- 新建规则版本；
- 停用类型；
- 查看历史版本；
- 查看适用流程；
- 查看使用该类型的合作记录。

## 11. 数据模型概念

### app_user

```text
id
username
email
display_name
password_hash
role                 -- admin / member
status               -- active / disabled
created_at
last_login_at
```

合作记录、公司、到账和导入批次都带有记录归属。所有角色只能查询本人数据；管理员只能额外管理账号和共享项目类型/规则。

### cooperation_record

```text
id
owner_id
organization_id
company_id
project_type_id
rule_version_id
record_date
work_status
completion_date
publicity_status
publicity_date
collection_status
advance_date
full_payment_date
participation_mode
my_ratio
standard_performance
my_due_amount
manual_override_reason
created_at
updated_at
```

### project_type_rule

```text
id
project_type_id
version
standard_performance
default_ratio
publicity_required
collection_stage
payout_pattern
effective_from
effective_to
enabled
```

### payout_batch

```text
id
cooperation_record_id
batch_name
trigger_type
planned_ratio
planned_amount
status
```

### payout_record

```text
id
cooperation_record_id
payout_batch_id
amount
received_date
notes
```

### performance_change

记录标准绩效、个人比例、本人应得金额、里程碑日期和到账信息的调整/作废历史及原因。

### import_batch

记录每次导入的来源、所选写入结果和新建记录 ID；V1 不提供批次浏览/整批撤销入口。

## 12. 统计口径

```text
已到账 = 所有个人到账记录金额之和
待到账 = 我的应得绩效 - 已到账
```

当“我的比例”或“我的应得绩效”未知时，待到账显示“待确认”，不显示为0。

进行中的合作记录可以显示潜在标准绩效，但不计入已完成收益统计。

## 13. MVP 验收标准

- 只输入合作公司和项目类型即可创建记录；
- 创建时自动使用北京时间当前日期；
- 项目类型可以带出标准绩效和默认流程；
- 项目类型规则支持版本；
- 历史记录不会随规则修改而变化；
- 同公司同类型重复时只提示，不自动合并；
- 支持完成、公示、预付款、全款四类里程碑；
- 预付款可以触发第一批发放节点；
- 发放比例未知时可以保存；
- 实际到账可以先录金额和日期；
- 修改绩效金额/比例和更正里程碑日期必须填写原因，并可查看记录级历史；到账纠错保留原行（作废并重录）；
- 支持多笔个人到账；
- 公司收款不要求记录具体金额；
- Dashboard 可以区分进行中、待公示、待收款、待发放和已结清；
- Excel/剪贴板导入不依赖 AI；
- 导入前可以预览、处理重复项和错误项；
- 支持数据导出和备份。
- 公网登录，管理员可以创建、禁用和重置成员账号；
- 所有角色（含管理员）只能看到自己的业务数据；管理员可以管理账号和共享项目类型/规则；
- 每日检查数据版本，只有发生变化时才生成备份，并保留30天。

## 14. 产品成功标准

用户可以在一分钟内录入一条基础合作记录，并且在打开首页时能够明确看到：

```text
哪些还在做
哪些已经完成
哪些还要公示
哪些公司已经收款
哪些绩效已经到账
哪些绩效仍然没有到账
```
