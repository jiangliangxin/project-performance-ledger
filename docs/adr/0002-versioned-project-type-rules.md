# Drive defaults from versioned project type rules

**Status**: accepted

Project types own the default standard performance amount, publicity requirement, collection requirement, and payout pattern. Each cooperation record snapshots the rule version used at creation, while exceptional records may override those defaults with a recorded reason; later rule changes never rewrite historical records.

