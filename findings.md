# 发现与决策

## 需求

- 产品是个人项目绩效收益记账工具，不是完整项目管理系统。
- 用户通常只记录合作公司和项目类型，不强制填写项目名称。
- 同一公司同一类型多年后再次合作，应创建新的合作记录；重复时提示但不自动合并。
- 项目类型规则需要维护标准绩效、是否公示、公司收款节点和发放模式。
- 标准绩效默认按100%独享计算；多人项目只先记录本人的比例，比例可以未知。
- 公司收款不记录实际金额，只记录预付款、全款及日期。
- 预付款可能触发部分个人发放；比例和金额未知时允许后补。
- 项目完成、公示、公司收款、个人到账必须独立记录。
- 未填写记录日期时，使用 `Asia/Shanghai（UTC+8）` 当前日期；完成日期后续补充。
- Excel/CSV/剪贴板导入采用确定性规则，不使用 AI。
- 第一版不做提醒、附件、成果管理和团队任务协作；支持管理员创建少量成员账号，但成员数据按用户隔离。

## 研究发现

- 当前工作目录没有应用代码、`package.json` 或现有技术栈，需要按从零开发设计。
- 已有 PRD、CONTEXT 和 4 条 ADR，可作为业务基线。
- 项目类型规则不能只用一个“收款要求”字段表达“预付款触发部分发放、全款触发剩余发放”，需要拆分收款节点和发放模式。
- 官方文档确认 Vue 3 是组件化 UI 框架，可用于构建 SPA；Vue 官方也提供测试指导。FastAPI 官方文档覆盖独立部署策略；Docker Compose 官方文档适合定义和运行多容器应用；PostgreSQL 官方文档提供事务语义，可用于导入提交和业务写入的原子性。
- SQLite 官方文档说明其单文件、易部署、适合作为嵌入式数据库；同时它的并发写入能力不是面向高并发，因此适合当前最多约 4～5 人、低频写入的版本，未来出现高并发或多实例部署时再迁移到 PostgreSQL。

## 技术决策
| 决策 | 理由 |
|------|------|
| 采用模块化单体，而不是过早微服务 | 小团队、低并发系统需要低运维；未来扩展可在模块边界清晰的前提下拆分 |
| MVP 使用 SQLite，不使用 PostgreSQL | 最多约 4～5 人、低频写入、重视易部署和备份；保留 SQLAlchemy/Alembic 边界，未来可迁移 |
| MVP 不使用 Redis、消息队列和独立对象存储；公网部署保留现有 Nginx | 当前没有异步任务、文件附件或高并发需求；Nginx 仅负责 HTTPS、静态文件和 API 反向代理 |
| 公网部署使用 Nginx + systemd + FastAPI | Nginx 负责 HTTPS、静态文件和反向代理；systemd 管理单个 FastAPI 进程，避免引入 Docker 运维成本 |
| 支持管理员创建成员但按用户隔离 | 满足少量同事使用，同时保护个人收益记录，不提前建设复杂组织权限 |
| 金额、状态和导入均采用显式规则 | 绩效台账需要可复核，不能依赖不可解释推断 |
| 规则版本在合作记录上快照 | 防止调整项目类型后改变历史收益 |
| 个人到账使用独立记录表 | 支持预付款后的部分到账和后续多批次到账 |

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|
| 项目名称不是用户的主要识别方式 | 使用合作公司、项目类型和记录日期生成显示标识 |
| 公司收款与个人到账容易混淆 | 分离公司收款里程碑和个人到账记录 |
| 发放比例未知 | 支持空值/待确认，实际到账可先录入 |

## 资源

- [PRD.md](./PRD.md)
- [CONTEXT.md](./CONTEXT.md)
- [docs/adr/](./docs/adr/)
- [Vue 3 官方指南](https://vuejs.org/guide/introduction)
- [Vue 测试指南](https://vuejs.org/guide/scaling-up/testing)
- [FastAPI 部署文档](https://fastapi.tiangolo.com/deployment/)
- [Docker Compose 官方文档](https://docs.docker.com/compose/)
- [PostgreSQL 事务文档](https://www.postgresql.org/docs/current/tutorial-transactions.html)

## 本轮架构审查新增事实

- 用户确认系统部署在公网服务器，已有 Nginx 和子域名，因此“仅本机/局域网”假设失效；需要登录、HTTPS、反向代理和备份策略。
- 用户确认当前最多约 4～5 人使用，功能范围保持当前台账需求，不引入团队任务协作、通知、AI 和附件等非必要模块；成员记录相互隔离。
- 用户倾向 SQLite；其适用前提应明确为单机、单个应用实例、低写入并发、数据库文件位于持久化磁盘，并配套可验证的自动备份。
- 用户确认默认直接在服务器运行 FastAPI，不以 Docker 为必需；同服务器每日检查数据变化，有变化才备份，保留 30 天。
- React 官方文档说明 React 可从零构建客户端 SPA，但从零搭建时需要自行选择路由、数据获取和样式等方案；官方新项目文档更强调按项目约束选择框架或构建工具。因此 Vue/React 的结论应基于本项目的个人维护成本，而不是把任一者当作后端架构要求。

## 视觉/浏览器发现

- 无浏览器或视觉审查；本任务为业务和技术文档设计。

## 2026-09-23 上线准备审计

### 结论

- 当前提交为 641c1c8，审计开始时 Git 工作区干净。
- 核心 MVP 代码存在，自动测试和构建通过；但它还没有在真实服务器部署，不能视为已达到公网生产上线标准。
- 架构不需要重做。上线整改按“权限隔离 → 结算正确性 → 导入完整性 → 纠错审计 → 公网运维”排序；真实服务器部署和恢复演练仍需在目标机验收。

### 本轮验证

- 后端 pytest -q：4 passed。
- 前端 npm run build：vue-tsc 和 Vite 构建通过。
- Python compileall：通过。
- 在隔离 SQLite 文件上执行 alembic upgrade head 与 alembic check：通过，无模型差异。
- 未做真实公网部署、真实浏览器端到端测试、Nginx/systemd 目标机验证或数据库恢复演练。
- 规则状态探针复现：配置 advance_and_full + single，已有预付款和等于应得的个人到账、没有全款时，状态仍返回 settled。
- 导入解析探针复现：-3000 被解析为负金额，150% 被解析为 150；当前导入提交路径没有金额非负和比例 0–100 的二次校验。

### 上线前阻断项（P1）

1. **结算规则组合未约束。** 项目类型界面允许任意组合，但状态机只完整覆盖开发方案中列出的组合；上述探针能在未收全款时显示“已结清”。需后端拒绝非法组合，并覆盖合法组合及边界的领域测试。
2. **导入存在金额/比例数据完整性风险。** 导入金额和比例绕过请求模型范围校验；缺少有效类型规则时，预览仍可能将行标为有效，提交才跳过。还需检测同一批导入内部的重复项。
3. **记录纠错能力不完整。** PRD/CONTEXT 承诺单条记录可以覆盖公示和公司收款流程，但 API/UI 只覆盖金额与比例。里程碑设置后页面隐藏动作，无法在 UI 更正日期；到账有 PATCH/void API 但没有页面操作。PATCH 中传 null 会被当成“未修改”，比例/人工金额不能清空。
4. **绩效调整缺少可审计闭环。** UI 修改金额/比例不要求原因；API 的编辑路径使用固定原因；performance_changes 没有查询接口或页面展示。特殊情况的创建值也未形成清晰的调整历史。
5. **管理员越权访问他人业务数据。** 用户已确认管理员也只能访问自己的合作公司、合作记录、到账、导入、Dashboard 和导出；管理员只额外管理账号及共享项目类型/规则。当前服务端存在管理员绕过 owner 校验、读取或修改其他成员记录的路径，这是上线前必须修复的隐私阻断项。
6. **公网部署模板不完整且未演练。** Nginx 示例没有证书路径，systemd 启动单元没有部署迁移步骤；应用 lifespan 每次启动调用 create_all()，而 create_all 不会为已有库应用后续 schema migration。需明确唯一迁移流程并在目标机验证。
7. **备份保留规则与需求不一致。** 脚本只保留最近 30 个备份文件，不是按 30 天删除；且当前没有恢复脚本/步骤和目标服务器恢复演练。同机备份是已确认方案，但不能防止整台服务器或磁盘损坏。
8. **公网登录/文件上传缺少基础限制。** 登录没有尝试频率限制，上传读取整个文件到内存且没有应用层大小限制；生产安全配置依赖人工把 JIZHANG_COOKIE_SECURE 从示例的 false 改为 true。
9. **比例未知但已有到账时的当前节点错误。** 用户已确认此时仍应显示“待确认个人比例”，已到账金额单独展示且待到账未知；当前状态计算会显示“部分发放”，不符合台账核心口径。

### 重要但可排在 P1 后的问题（P2）

- 重复检查只命中相同公司、类型、精确记录日期；应按当前用户的全部历史和当前批次提示相同公司/类型，不按日期自动去重。多年后仍允许用户确认新建。
- 用户已确认导入预览应允许选择有效行、错误行不可选，所选有效行在一个事务中提交；当前没有逐行选择 UI。PRD 另要求查看/撤销批次，但实际没有对应 UI/API。
- 规则管理 UI 可新建类型和规则版本，但缺少类型改名/停用等维护交互；列表筛选缺记录年度、公示/公司收款/发放状态等 PRD 字段。
- V1 代码仍接受“已取消”工作状态，与用户已确认的“取消语义不做、仅归档整理”不一致；需移除取消状态并确保归档不改变金额和到账历史。
- 到账超过应得金额允许保存，但界面没有 PRD 要求的超额提示；API 若传非法枚举（参与方式、批次类型、公司/类型状态）仍有部分路径可落库。
- 当前仅有 4 个后端集成测试，没有领域组合矩阵、前端交互测试、浏览器端到端或目标环境 smoke test。

### 已确认的业务决策（2026-09-23）

1. 管理员也只能访问自己的业务记录；其额外职责仅为管理账号和共享项目类型/规则。
2. 导入预览由用户选择有效行，错误行不可选；重复只提示、不自动合并；选中的有效行整体事务提交，提交失败整批回滚。
3. V1 不提供“已取消”状态，以归档整理记录；归档不得改变绩效、里程碑或到账事实。
4. 本人比例/应得未知时，即使已有到账，状态仍为“待确认个人比例”；展示已到账，不计算待到账。

---
*本审计为静态代码/文档审查加现有自动检查；不是渗透测试、代码依赖漏洞扫描或生产环境验收。*

## 2026-09-23 整改实施定位

- 权限根因在 `apps/api/app/main.py`：`require_record`、公司/列表查询、到账修改/作废和 Dashboard/导出查询存在基于 `admin` 的 owner-scope 旁路；需统一为当前用户 owner 范围，并做双向 API 回归。
- 状态根因在 `apps/api/app/domain.py`：应得未知且已到账时先返回 `partial_paid`；`advance_and_full + single` 也未在规则写入时被拒绝。先固定合法规则组合，再用状态矩阵测试覆盖。
- 导入接口已接受可选 `row_numbers`，但前端始终提交全部有效行；后端金额/比例范围与实际匹配规则校验不足，重复检查目前绑定精确记录日期。应保留预览逻辑，补强校验与选择提交，不重写解析器。
- 归档接口和 `archived_at` 字段已存在；需要移除 V1 的 `cancelled` 工作状态输入和显示，不把归档当作财务状态变更。
- 现有测试主要集中在 `apps/api/tests/test_api.py` 的 4 个 API 场景；新增领域状态矩阵和权限/导入边界测试，不依赖已有固定样例名称。
- 代码定位已复核：owner 旁路存在于 `require_owned_company`、`require_record`、公司/记录列表、到账修改/作废和 `scoped_records`；`ImportCommitRequest.row_numbers` 已存在，但页面未提供选择控件。
- 当前服务在 FastAPI lifespan 中调用 `create_all()`；部署整改需移除生产启动时隐式建表，改为明确执行 Alembic migration，再做服务器验收。
- 规则管理页面额外提供了语义不清的 `manual` 发放模式，而 V1 已文档化的规则只有四种组合；新规则应只允许这四种，历史记录中的旧值保持可读，不做破坏性数据改写。
- 旧版可能已有 `cancelled` 记录。修复先阻止新建/编辑继续写入该状态，并提供归档入口；不在没有数据盘点的情况下批量重写历史工作状态。
- 导入选行必须同时覆盖粘贴和文件两条提交路径；文件提交需要在 multipart 请求中附带用户确认的行号，后端重新预览并验证所选行，不能信任客户端仅回传“有效”标记。
- 导入整改已实现：UTF-8 内容 5 MB、文件 5 MB、XLSX 解压 25 MB、最多 1 万数据行；预览拦截非法金额、非法比例、缺失类型/日期规则，提示跨日期（含归档）历史重复和批内重复。提交重新解析校验所选行，仅提交用户选中的有效行，数据库异常时回滚整批；文件表单与粘贴 JSON 均传明确行号。三条定向 API 测试及前端生产构建通过。
- 归档整改已实现：默认列表排除归档记录，`archived=true` 可浏览并恢复；详情回传归档时间，归档/恢复只改变展示范围，不改金额、流程节点或到账。
- 取消语义已关闭：新建/编辑只允许 `in_progress`、`completed`；历史库内若有旧 `cancelled` 值仍保持只读可见，不做无审批批量改写。
- 财务纠错已实现：Record PATCH 用 Pydantic `model_fields_set` 区分“未传”和“明确清空”；改比例/标准/应得金额时必须写原因，`performance_changes` 有按记录查询 API 和详情页历史。里程碑可按字段设置、纠正或清空，日期改动带原因并记录历史。
- 到账 UI 采用“作废原记录 + 新增正确记录”纠错；直接 PATCH 也要求原因并逐字段记录历史，已作废记录不可再次编辑/作废。作废同样必须填写原因，调整历史覆盖金额、日期、批次、备注与作废事件。
- 整改中期验证：后端 14 passed，前端 `npm run build` 通过；最终 24 项测试和部署边界见下方更新。

## 2026-09-23 整改结果与剩余上线门槛

### 已在本地修复并验证

- 管理员和成员的业务查询/变更统一按本人 `owner_id` 隔离；管理员仅额外管理账号和共享类型/规则。
- 结算状态保持“公司收款、个人到账”分离；只接受有明确定义的四种规则组合；应得金额未知时，即使有到账仍待确认比例，待到账不伪算为 0。
- 导入预览校验有限金额、比例、类型规则和文件边界，提示历史/批内重复；修复“公司名称”表头识别；有效行可选择，错误行不可选，服务端重验并单事务提交，失败整体回滚。
- 记录、绩效、里程碑与到账的纠错要求原因并保留历史；个人到账更正采用作废原记录后重录。归档只隐藏记录，可恢复且不更改业务事实。
- 生产配置强制 Secure Cookie、拒绝通配 CORS，加入单进程登录限速和有界文件读取；服务启动明确执行 Alembic，而不是运行时 `create_all()`。
- 备份保留期按 30 天清理，增加可验证的恢复脚本和恢复前安全副本；部署示例统一虚拟环境路径并提供公网验收清单。
- 公司/项目类型 API 限制状态枚举、空白名称和重复改名；新建规则不再提供未定义的 `manual` 组合，旧值只作兼容读取。

### 不应误报为已上线

- 未访问目标公网服务器；DNS、有效证书、Nginx/systemd 配置、真实 HTTPS 登录和公网端口隔离仍须在目标机执行 `docs/GO_LIVE_CHECKLIST.md`。
- 备份和恢复仅在本地隔离 SQLite 上测试；目标机每日 systemd timer、30 天轮转和完整恢复演练尚未执行。
- 未完成真实浏览器端到端、依赖漏洞扫描、渗透或独立安全测试。
- 非上线阻断的 V1 缺口仍有：导入批次浏览/整批撤销入口、更多列表筛选、项目类型改名/停用的前端交互。API 已能拒绝非法状态/重复改名，但完整的类型维护交互仍待后续补齐。

最终本地结果：`apps/api` pytest 24 passed；Vue 类型检查与 Vite 构建通过；Python compileall 通过；隔离 SQLite `alembic upgrade head` 和 `alembic check` 通过（No new upgrade operations detected）；`git diff --check` 通过。另验证登录 Cookie 含 `HttpOnly`、`Secure`、`SameSite=Lax`，超 5 MB 导入文件返回 HTTP 413。

---
*每执行2次查看/浏览器/搜索操作后更新此文件*
*防止视觉信息丢失*
