# 任务计划：个人项目绩效台账上线整改与验收

## 目标

按用户确认的业务边界修复上线审计发现的问题，补齐测试和部署验收材料。目标是让代码达到可在目标服务器进行上线验收的状态；不把本地测试等同真实公网验收。

## 当前阶段

阶段 9 本地整改与回归已完成；真实目标服务器验收尚未执行。

## 各阶段

### 阶段 1：需求与发现
- [x] 读取并确认现有 PRD、领域术语和架构边界
- [x] 确认从零开发，没有既有代码约束
- [x] 将关键发现记录到 findings.md
- **状态：** complete

### 阶段 2：规划与结构
- [x] 确定推荐技术架构和部署边界
- [x] 设计领域模型、数据表和状态计算
- [x] 设计 API、页面和导入协议
- [x] 记录方案取舍及实施风险
- **状态：** complete

### 阶段 3：方案文档
- [x] 编写 DEVELOPMENT_PLAN.md
- [x] 编写接口、数据迁移和验证要求
- [x] 编写分阶段任务、验收标准和回滚方案
- **状态：** complete

### 阶段 4：审查与验证
- [x] 检查开发方案与 PRD/CONTEXT/ADR 一致
- [x] 检查轻量方案与当前小团队低并发范围一致
- [x] 记录最终静态审查结果
- **状态：** complete

### 阶段 5：确认后的方案同步
- [x] 纳入公网部署、Nginx、登录和备份约束
- [x] 纳入管理员创建成员账号和按用户隔离
- [x] 重新核对 SQLite 在 4～5 人低并发下的边界
- [x] 更新 PRD、CONTEXT、开发方案和 ADR
- [x] 做最终文档一致性检查
- **状态：** complete

### 阶段 6：MVP 应用开发
- [x] 建立 Vue + FastAPI + SQLite 工程骨架
- [x] 实现登录、管理员创建成员和按用户隔离
- [x] 实现公司、项目类型、规则版本和合作记录
- [x] 实现完成、公示、公司收款和个人到账流程
- [x] 实现 Dashboard、确定性导入和 CSV 导出
- [x] 实现每日变更检测备份脚本
- **状态：** complete

### 阶段 7：验证与交付
- [x] 后端测试和数据库迁移验证
- [x] 前端类型检查、构建和关键交互验证
- [x] 启动说明、环境变量和备份恢复说明
- [x] 记录未完成范围和下一阶段事项
- **状态：** complete

### 阶段 8：上线准备审计
- [x] 对照 PRD、CONTEXT、ADR 检查功能和领域模型实现
- [x] 检查认证、数据隔离、导入、状态计算、备份和部署配置
- [x] 运行现有后端测试、前端构建、Python 编译和 Alembic 迁移检查
- [x] 用具体输入复现流程组合和导入边界问题
- [x] 将证据、风险等级和未决问题记录到 findings.md
- [x] 与用户确认管理员可见/可改范围、导入批次行为、取消记录口径和比例未知时的状态
- **状态：** complete

### 阶段 9：上线整改实施
- [x] 将四项业务决策同步到 CONTEXT、PRD、开发方案和 ADR
- [x] 修复管理员越权访问，所有业务读写、Dashboard 和导出均严格按 owner 隔离
- [x] 修复结算状态机，拒绝不支持的规则组合；未知比例有到账时仍待确认
- [x] 取消 V1 的“已取消”业务状态，统一为独立归档语义
- [x] 加固导入：金额/比例/类型规则校验、历史与批内重复提示、可选有效行、整批原子提交
- [x] 补齐记录/里程碑/到账纠错、人工调整原因和历史查询
- [x] 加固公网安全、迁移流程、备份保留与恢复脚本；本地完成恢复逻辑测试
- [x] 增加领域/API/UI 相关回归测试并运行后端测试、前端检查与构建
- [x] 明确真实服务器部署、HTTPS、Nginx/systemd 和恢复演练的验收清单
- **状态：** complete；公网部署、TLS 和目标机恢复演练仍待执行

## 关键问题
1. 何时从 SQLite 升级 PostgreSQL。
2. 何时从 owner 隔离升级到组织、成员、工作空间和共享数据。
3. 何时需要从直接部署升级到 Docker、多实例或 PostgreSQL。

## 已做决策
| 决策 | 理由 |
|------|------|
| 合作公司 + 项目类型 + 一次合作作为合作记录 | 用户不按项目名称记账，重复合作必须独立核算 |
| 项目类型规则版本化 | 绩效标准和流程未来会变化，历史记录不能被追溯改写 |
| 完成、公示、公司收款、个人到账分离 | 四者业务含义不同，预付款只触发部分发放资格 |
| 预付款和全款只记录节点/日期，不记录公司金额 | 用户不管理公司实际收款金额 |
| 小团队账号、数据按用户隔离 | 满足管理员创建少量同事账号的需求，同时避免提前建设共享协作和细粒度权限 |
| Excel/剪贴板采用规则解析，不使用 AI | 绩效金额数据需要可预测、可复核的导入结果 |
| 第一版采用 Vue + FastAPI + SQLite 轻量单体 | 满足个人起步、4～5 人低并发记账和导入需求，降低部署与维护成本 |
| Nginx + systemd 直接部署 | 服务器已有 Nginx；直接部署比 Docker 少一层数据卷和权限维护 |
| PostgreSQL、Redis、队列和细粒度企业权限推迟到扩展阶段 | 当前没有高并发、异步任务或共享协作需求 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Python `compileall` 尝试写入 macOS 受限缓存目录 | 1 | 改用任务专用的 `PYTHONPYCACHEPREFIX` 临时路径重新验证 |
| `npm install` 长时间无输出 | 1 | 停止无反馈进程，先继续静态检查并改用受控依赖安装/验证 |
| `uv sync` 默认缓存目录无权限 | 1 | 改用任务专用的 `UV_CACHE_DIR` 临时路径 |
| Python 3.9 无法解析 SQLAlchemy 模型中的 `str | None` 注解 | 1 | 将 ORM 可空注解改为 `typing.Optional`，重新导入验证通过 |
| Python 3.9 系统构建缺少 `hashlib.scrypt` | 1 | 改用带随机盐和 310000 次迭代的 PBKDF2-HMAC-SHA256 |
| 登录接口首次测试返回 422 | 1 | 将 FastAPI 响应参数标注为 `Response`，Cookie 登录测试通过 |
| SQLite 临时备份文件用 URI 只读校验失败 | 1 | 校验阶段改用显式文件路径连接，备份和无变化跳过均通过 |
| Alembic check 在清理后的空数据库上提示 target database not up to date | 1 | 先执行 `alembic upgrade head`，再 check 通过；测试数据库随后清理 |
| 沙箱禁止 Uvicorn 绑定 `127.0.0.1:8000` | 1 | 使用 FastAPI TestClient 完成接口验证；未宣称真实端口启动验证通过 |
| 最终验证从仓库根目录调用后端测试/Alembic 导致 `app` 导入路径不匹配；一次 `uv run` 编译使用受限默认缓存 | 3 次验证命令 | 从 `apps/api` 使用项目虚拟环境执行 pytest、Alembic 和 compileall，全部通过 |

## 备注
- 本地整改和回归已完成；系统尚未部署到真实公网服务器，不能据此宣称已通过生产上线验收。
- 真实目标机的 DNS/TLS、Nginx/systemd、每日定时备份和恢复演练见 `docs/GO_LIVE_CHECKLIST.md`，需在服务器环境执行。
